/**
 * Use this file to maybe define the settings of our server
 * For now its local in port 8080 but if we deploy it we need to change this info
 */

module.exports = {
    serverHost: "localhost:8080"
}